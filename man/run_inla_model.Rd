% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_inla_model.R
\name{run_inla_model}
\alias{run_inla_model}
\title{Fit a general model with INLA}
\usage{
run_inla_model(
  dataset = NULL,
  formula,
  family = "binomial",
  Ntrials = NULL,
  temporal_resolution = c("daily", "hourly"),
  iso3 = NULL,
  admin_level = NULL,
  admin_name = NULL,
  input_dir = "data/proc",
  control.family = list(link = "logit"),
  control.compute = list(dic = TRUE, waic = TRUE, cpo = TRUE, config = TRUE),
  control.predictor = list(compute = TRUE, link = 1),
  inla_args = list(),
  write_output = TRUE,
  output_path = "data/proc",
  verbose = TRUE
)
}
\arguments{
\item{dataset}{An \code{inla_data_prep} object, a path to a saved preparation
object, or \code{NULL}.}

\item{formula}{A formula or single character string accepted by
\code{\link[INLA:inla]{INLA::inla()}}.}

\item{family}{INLA likelihood family. Defaults to \code{"binomial"}.}

\item{Ntrials}{Optional binomial trial counts. When \code{NULL}, one trial per
observation is used.}

\item{temporal_resolution}{Either \code{"daily"} or \code{"hourly"}.}

\item{iso3, admin_level, admin_name}{Location identifiers used when
\code{dataset = NULL}.}

\item{input_dir}{Directory containing prepared INLA data.}

\item{control.family}{List passed to \code{INLA::inla(control.family = ...)}.}

\item{control.compute}{List passed to \code{INLA::inla(control.compute = ...)}.}

\item{control.predictor}{List passed to
\code{INLA::inla(control.predictor = ...)}.}

\item{inla_args}{Additional named arguments passed to \code{\link[INLA:inla]{INLA::inla()}}.}

\item{write_output}{Whether to save the fitted model.}

\item{output_path}{Output directory or RDS filename.}

\item{verbose}{Whether to emit progress messages.}
}
\value{
A fitted \code{inla} object.
}
\description{
Fits a user-supplied INLA formula using data prepared by
\code{\link[=prepare_inla_data]{prepare_inla_data()}}. External objects referenced by the formula, such as
hyperparameter lists, are resolved from the environment in which the formula
was created.
}
\examples{
\dontrun{
landcover_iid_hyper <- list(
  prec = list(
    prior = "pc.prec",
    param = c(1, 0.05)
  )
)

occupancy_formula <- presence ~
  f(
    sea_day_id,
    model = "rw2",
    cyclic = TRUE,
    values = 1:365,
    constr = TRUE,
    scale.model = TRUE
  ) +
  ndvi_z +
  elev_z +
  f(
    landcover_class,
    model = "iid",
    constr = TRUE,
    hyper = landcover_iid_hyper
  )

fit <- run_inla_model(
  dataset = inla_data,
  formula = occupancy_formula
)
}

}
